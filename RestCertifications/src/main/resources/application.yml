spring:
  security:
    user:
      name: dntruon
      password: dnt123
server:
  ssl:
    key-store: classpath:ssl/keystore.jks
    key-store-password: changeit
    key-alias: localhost
    enabled: true 
  port: 8443
  servlet.context-path: /rest-pki